{
  "build": {
    "prebuild": "apt-get update && apt-get install -y libicu-dev libzip-dev && docker-php-ext-configure intl && docker-php-ext-install intl zip"
  },
  "deploy": {
    "healthcheck": {
      "path": "/",
      "timeout": 100
    },
    "restartPolicyType": "on_failure",
    "startCommand": "php artisan migrate --force && php artisan optimize && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}